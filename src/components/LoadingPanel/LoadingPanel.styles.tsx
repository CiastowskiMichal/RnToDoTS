import { StyleSheet } from 'react-native';

const item = StyleSheet.create({
  loadingPanel: {
    textAlign: 'center',
    color: '#2E4172',
    fontSize: 30,
    fontStyle: 'normal',
    marginTop: 30,
    marginBottom: 10,
  },
});

export default item;
